<%= render :partial => 'spree/admin/shared/web_contents_menu' %>

<% content_for :page_title do %>
  <%= t(:sweepstakes) %>
<% end %>

<% content_for :page_actions do %>
  <li>
    <%= button_link_to t(:back_to_sweepstakes_list), spree.admin_sweepstakes_path, :icon => 'icon-arrow-left' %>
  </li>
<% end %>

<%= form_for [:admin, @sweepstake] do |f| %>

  <%= render :partial => 'form', :locals => { :f => f } %>
  <fieldset class="no-border-top">
    <br>
    <div class="filter-actions actions" data-hook="buttons">
      <%= button t(:create), 'icon-ok' %>
    </div>
  </fieldset>
<% end %>
